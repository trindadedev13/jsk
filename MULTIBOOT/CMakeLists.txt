SET (SOURCES
        BOOT.ASM)

INCLUDE (${CMAKE_SOURCE_DIR}/CMAKE/FIXEXTS.CMAKE)

ADD_LIBRARY (MULTIBOOT
        OBJECT
        ${SOURCES})

TARGET_INCLUDE_DIRECTORIES (MULTIBOOT PRIVATE
        ${INCLUDE_DIR})